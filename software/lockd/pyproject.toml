[project]
name = "lockd"
version = "0.1.0"
description = ""
requires-python = ">=3.9"
dependencies = [
    "pillow>=11.0.0",
    "pycryptodome>=3.21.0",
    "pyserial>=3.5",
]

[dependency-groups]
dev = [
    "mock>=5.1.0",
    "ruff>=0.8.2",
]

[project.scripts]
lockd = "lockd.lockd:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
